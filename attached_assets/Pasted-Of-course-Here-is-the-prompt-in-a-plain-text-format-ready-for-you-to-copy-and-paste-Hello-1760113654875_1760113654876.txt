Of course. Here is the prompt in a plain text format, ready for you to copy and paste.

```
Hello! I need help modifying my MERN stack farm management application. I want to add functionality to the "Inventory" and "Order" sections.

Right now, the inventory data is static. I want to connect it to a MySQL database and make the "Order" button functional.

Here are the detailed requirements:

1. Database Setup (MySQL)

Please set up a MySQL database connection. Create two tables with the following schemas:

1.  Inventory Table: To store the stock information.
    * id (Primary Key, INT, AUTO_INCREMENT)
    * itemName (VARCHAR)
    * quantity (INT)
    * unit (VARCHAR, e.g., 'kg', 'ltr')
    * status (VARCHAR, e.g., 'In Stock', 'Low Stock', 'Out of Stock')

2.  Orders Table: To store all the orders placed by the user.
    * orderId (Primary Key, INT, AUTO_INCREMENT)
    * itemName (VARCHAR)
    * quantityOrdered (INT)
    * orderDate (TIMESTAMP, default to current time)
    * status (VARCHAR, e.g., 'Pending', 'Completed')

Please use Replit Secrets to store the database credentials (host, user, password, database name).

2. Backend API (Node.js & Express)

Create the following API endpoints to interact with the database:

* GET /api/inventory: Fetches all items from the Inventory table and sends them as a JSON response.
* POST /api/orders: Receives order data (e.g., itemName, quantityOrdered) in the request body, inserts a new record into the Orders table, and returns a success message.
    * (Optional but preferred) When a new order is created, also update the quantity in the Inventory table for the corresponding item.

3. Frontend Changes (React)

Please modify the React components to implement the new functionality:

1.  Connect Inventory to API:
    * Modify the component that displays the Inventory list.
    * Instead of using hardcoded data, use useEffect and axios (or fetch) to call the GET /api/inventory endpoint and display the data from the database.

2.  Create an Order Modal/Form:
    * When the user clicks the green "Order" button, a modal or a new form section should appear.
    * This form should have input fields for the item name and the quantity to order.
    * When the form is submitted, make a POST request to the /api/orders endpoint with the form data.
    * After a successful order, close the modal and show a success notification.

3.  Display Orders:
    * Create a new component or section on the dashboard to display a list of recent orders from the Orders table. You will need a new API endpoint like GET /api/orders for this.

Please ensure the code is clean and follows best practices. Thank you!
```